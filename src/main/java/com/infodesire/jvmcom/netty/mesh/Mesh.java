package com.infodesire.jvmcom.netty.mesh;

import com.infodesire.jvmcom.netty.file.FileClient;
import com.infodesire.jvmcom.netty.logging.LoggingClient;

public class Mesh {

    public static void main( String[] args ) {

//        // server
//        Mesh.start();
//
//        // client
//        Mesh mesh = Mesh.join();
//        LoggingClient logger = (LoggingClient) mesh.find( "logger" );
//        ConfigClient configs = (ConfigClient) mesh.find( "config" );
//        CachingClient caches = (CacheClient) mesh.find( "cache" );
//        FileClient files = (FileClient) mesh.find( "file" );

    }

}

